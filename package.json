{
  "name": "prism-desktop",
  "private": true,
  "version": "0.5.5",
  "type": "module",
  "workspaces": [
    "agent-sidecar",
    "web"
  ],
  "scripts": {
    "dev": "node scripts/dev-desktop.mjs",
    "build": "npm --prefix web run build && npm --workspace agent-sidecar run build && tauri build"
  },
  "dependencies": {
    "@tauri-apps/api": "2.10.1",
    "@tauri-apps/plugin-dialog": "^2.7.0",
    "@tauri-apps/plugin-shell": "^2.3.5"
  },
  "devDependencies": {
    "@tauri-apps/cli": "2.10.1"
  }
}
